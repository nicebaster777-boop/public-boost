# Чеклист для деплоя на хостинг

## Подготовка к деплою

### 1. Выбор VPS
- [ ] Выбрать провайдера (Timeweb, Selectel, DigitalOcean, etc.)
- [ ] Заказать VPS: 20 GB SSD, 2-4 GB RAM, 2 CPU
- [ ] Получить IP адрес и root доступ

### 2. Подготовка сервера
- [ ] Подключиться по SSH
- [ ] Обновить систему: `apt update && apt upgrade -y`
- [ ] Установить Docker и Docker Compose
- [ ] Настроить firewall (открыть порты 80, 443, 22)

### 3. Настройка проекта
- [ ] Клонировать репозиторий на сервер
- [ ] Создать `.env` файл из `env.example`
- [ ] Настроить все переменные окружения:
  - [ ] База данных (POSTGRES_*)
  - [ ] Redis (REDIS_*)
  - [ ] Секретные ключи (SECRET_KEY, ENCRYPTION_KEY)
  - [ ] SMTP настройки (если нужна отправка email)
  - [ ] CORS origins (домен сайта)

### 4. Деплой
- [ ] Запустить `docker-compose -f docker-compose.prod.yml up -d`
- [ ] Применить миграции БД
- [ ] Проверить, что все сервисы запущены: `docker-compose ps`
- [ ] Проверить логи: `docker-compose logs`

### 5. Настройка домена
- [ ] Настроить DNS записи (A запись на IP сервера)
- [ ] Настроить SSL сертификат (Let's Encrypt)
- [ ] Обновить CORS origins в `.env`

### 6. Проверка
- [ ] Открыть сайт в браузере
- [ ] Проверить регистрацию/вход
- [ ] Проверить работу API
- [ ] Проверить работу фронтенда

---

## Полезные команды

```bash
# Проверка статуса
docker-compose -f docker-compose.prod.yml ps

# Просмотр логов
docker-compose -f docker-compose.prod.yml logs -f

# Перезапуск сервисов
docker-compose -f docker-compose.prod.yml restart

# Остановка
docker-compose -f docker-compose.prod.yml down

# Обновление кода
git pull
docker-compose -f docker-compose.prod.yml up -d --build
```

---

## Документация

- `DEPLOY_FROM_SCRATCH.md` - **деплой с чистого листа (рекомендуется)**
- `DEPLOY_QUICK_GUIDE.md` - быстрый гайд по деплою
- `DEPLOYMENT.md` - подробная инструкция по деплою
- `HOSTING_REQUIREMENTS_REALISTIC.md` - требования к хостингу
- `EMAIL_SETUP.md` - настройка отправки email

---

**Удачи с деплоем! 🚀**
