# Деплой с чистого листа на хостинг

## ✅ Да, всё правильно! Начинаем с нуля

### Что нужно на сервере:
1. **Только Docker и Docker Compose** - всё остальное установится автоматически
2. **Код из Git** - выгрузим через `git clone`
3. **Файл .env** - создадим из примера и настроим

### Что НЕ нужно переносить:
- ❌ `venv/` (виртуальное окружение Python)
- ❌ `node_modules/` (зависимости Node.js)
- ❌ Локальные базы данных
- ❌ Локальные настройки

**Всё это Docker установит автоматически!**

---

## Пошаговый план деплоя

### Шаг 1: Подключение к серверу
```bash
ssh user@your-server-ip
```

### Шаг 2: Установка Docker (если еще не установлен)
```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Проверка
docker --version
docker-compose --version
```

### Шаг 3: Выгрузка кода из Git
```bash
# Создать директорию
mkdir -p /opt/public-boost
cd /opt/public-boost

# Клонировать репозиторий
git clone <your-repo-url> .

# Проверить, что код выгрузился
ls -la
```

### Шаг 4: Настройка .env файла
```bash
# Скопировать пример
cp .env.example .env

# Отредактировать
nano .env
```

**Обязательно изменить в .env:**
- `SECRET_KEY` - сгенерировать: `openssl rand -hex 32`
- `ENCRYPTION_KEY` - сгенерировать: `python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`
- `POSTGRES_PASSWORD` - придумать сильный пароль
- `REDIS_PASSWORD` - придумать сильный пароль
- `DEBUG=false` - обязательно для продакшена
- `CORS_ORIGINS` - добавить ваш домен (например: `https://yourdomain.com`)

### Шаг 5: Запуск (Docker установит ВСЁ автоматически!)
```bash
# Docker автоматически:
# 1. Скачает все базовые образы (Python, Node, PostgreSQL, Redis, Nginx)
# 2. Установит все зависимости Python из requirements.txt
# 3. Установит все зависимости Node.js из package.json
# 4. Соберет образы
# 5. Запустит все сервисы

docker-compose -f docker-compose.prod.yml up -d --build

# Проверить статус
docker-compose -f docker-compose.prod.yml ps

# Посмотреть логи
docker-compose -f docker-compose.prod.yml logs -f
```

### Шаг 6: Применение миграций БД
```bash
# Войти в контейнер backend
docker-compose -f docker-compose.prod.yml exec backend bash

# Применить миграции
alembic upgrade head

# Выйти
exit
```

### Шаг 7: Проверка работы
```bash
# Проверить API
curl http://localhost/api/v1/health

# Проверить, что все контейнеры работают
docker-compose -f docker-compose.prod.yml ps
```

---

## Что Docker делает автоматически при `docker-compose up --build`:

### Backend контейнер:
1. ✅ Скачивает образ Python 3.11
2. ✅ Устанавливает все пакеты из `requirements.txt`
3. ✅ Копирует код приложения
4. ✅ Создает необходимые директории

### Frontend контейнер:
1. ✅ Скачивает образ Node.js
2. ✅ Устанавливает все пакеты из `package.json`
3. ✅ Собирает приложение (`npm run build`)
4. ✅ Настраивает Nginx для раздачи статики

### База данных:
1. ✅ Скачивает образ PostgreSQL 16
2. ✅ Создает базу данных
3. ✅ Настраивает пользователя и пароль из .env

### Redis:
1. ✅ Скачивает образ Redis 7
2. ✅ Настраивает пароль из .env
3. ✅ Создает volumes для данных

---

## Итого: что нужно на сервере

### Минимум:
- ✅ Docker и Docker Compose
- ✅ Git (для клонирования репозитория)
- ✅ Доступ к интернету (для скачивания образов)

### НЕ нужно:
- ❌ Python (установится в контейнере)
- ❌ Node.js (установится в контейнере)
- ❌ PostgreSQL (запустится в контейнере)
- ❌ Redis (запустится в контейнере)
- ❌ Nginx (запустится в контейнере)

---

## Преимущества подхода "с чистого листа":

1. **Чистая система** - нет конфликтов с существующими пакетами
2. **Воспроизводимость** - одинаково работает везде
3. **Простота** - одна команда запускает всё
4. **Изоляция** - все зависимости в контейнерах
5. **Безопасность** - не засоряем систему

---

## Если что-то пошло не так:

### Очистка и перезапуск:
```bash
# Остановить все контейнеры
docker-compose -f docker-compose.prod.yml down

# Удалить все образы (если нужно пересобрать)
docker-compose -f docker-compose.prod.yml build --no-cache

# Запустить заново
docker-compose -f docker-compose.prod.yml up -d
```

### Просмотр логов для отладки:
```bash
# Все логи
docker-compose -f docker-compose.prod.yml logs

# Логи конкретного сервиса
docker-compose -f docker-compose.prod.yml logs backend
docker-compose -f docker-compose.prod.yml logs frontend
docker-compose -f docker-compose.prod.yml logs db
```

---

## ✅ Итоговая формула:

```
Чистый сервер 
  + Docker 
  + Git clone 
  + .env файл 
  + docker-compose up 
  = Работающее приложение
```

**Всё остальное Docker сделает сам!** 🚀
